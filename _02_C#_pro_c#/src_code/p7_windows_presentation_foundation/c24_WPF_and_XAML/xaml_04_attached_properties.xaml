<Page
xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Canvas Height="200" Width="200" Background="LightBlue">
        <Ellipse Canvas.Top="40" Canvas.Left="40" Height="20" Width="20" Fill="DarkBlue" />
        <!--

        + attached property
        any property can use this "property-element syntax" pattern
            ```xaml
                <ParentElement>
                    <ChildElement ParentElement.PropertyOnParent = "Value">
                </ParentElement>
            ```



        + most common use of attached property
            - positions UI elements within one of the WPF layout manager classes(Grid, DockPanel, etc.,)

        + note
            >> this is NOT an all-purpose syntx that can be applied to "any property of any parent"

         -->
    </Canvas>

</Page>
