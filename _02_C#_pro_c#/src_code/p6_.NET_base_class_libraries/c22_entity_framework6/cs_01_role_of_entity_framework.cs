using System;

namespace RoleOfEntityFramework6
{
    class Program
    {
        static int Main(string[] args)
        {
            // EF
            {
                /*

                + what
                    - a new component of the ADO.NET API called the "Entity Framework"(or simply, EF);

                    - conceptually, rather than treating a batch of data as a colletion of rows and columns, u can operate on a collection of strongly typed objects termed "Entity";

                + why
                    - "Entity" are natively LINQ aware
                    - u can query against them using the same LINQ grammar
                    - EF runtime engine translates your LINQ queries into proper SQL queries;

                */

            }

            // P905 - P953
            {

                // need AutLot database, skip

            }

            return 0;
        }



    }
}
