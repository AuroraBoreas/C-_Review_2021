using System;

namespace CustomNamespace
{
    class Program
    {
        static int Main(string[] args)
        {
            // custom class libraries(*.dll files)
            {
                /*
                
                + what
                    - isolate reusable programming logic into "custom class lib"
                    - can be shared among applications
                
                
                */ 

                // see folder "custom_namespace01"


            }

            // resolve name clash
            {

                // see folder "custom_namespace02"

            }

            return 0;
        }

        
    }
}